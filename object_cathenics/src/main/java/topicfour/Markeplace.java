package topicfour;

public class Markeplace {
    /**
     This example show the behavior for topic four that is "First class collections",
     in other words, if you have the collection that you should to find element, or interact with them,
     so is better you create one class for to implement this.
     */

    Product arroz = new Product(1,"arroz");
    Products products = new Products(arroz);
}
